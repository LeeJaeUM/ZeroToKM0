using UnityEngine;

// 막대 형태의 재료
// 치즈, 베이컨
public class StripIngredient : SkewerIngredient
{
    public enum CurrentShape
    {

    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
